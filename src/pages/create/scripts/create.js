import { getCache } from '../../../global-modules/cache.js';

window.onload = () => {
    // console.log(await getCache('server') );
    // getCache('server').then(result => document.write(result.server));
}